# The Social Media Character Counter

Future Improvements and V2 Ideas
- Dynamic Warning System: Adding a feature that explicitly prints a warning like [COVER LIMIT] if the remaining characters drop below zero when I cover conditional logic.
- Smart Truncation: Add an option that automatically cuts off the draft at exactly 280 characters and returns a clean, compliant snippet to the user.
- Hashtag & Mention Tracker: Upgrade the script to count how many hashtags (#) or the user handles (@) are present in the text block.
